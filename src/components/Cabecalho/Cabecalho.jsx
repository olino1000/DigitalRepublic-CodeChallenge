import styled from 'styled-components';
import { corPrincipal } from '../../styles/variaveis';

const StyledCabecalho = styled.header`
	background-color: ${corPrincipal};
	display: flex;
	padding: 0 15vw;
	height: 10vh;
`;

export default StyledCabecalho;
