import styled from 'styled-components';
import { corTexto } from '../../styles/variaveis';

const StyledTitulo = styled.h1`
	color: ${corTexto};
	padding: 25px 0;
	text-align: center;
`;

export default StyledTitulo;
